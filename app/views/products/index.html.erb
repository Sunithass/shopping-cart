<!DOCTYPE html>
<html>
<head>
<style>
body 
{
background-color:#D9D6F3;
padding-top:10px;
padding-bottom:10px;
border-bottom:2px solid;
}
h1
{
padding-left:5.5em;
color:#282;
text-align:center;
font:small-caps 40px "Times New Roman", serif";
padding-top:5em;
}
table
{
padding-left:20em;
}
tr.list-line-odd
{
background-color:#78C1E6;
}
tr.list-line-even
{
background-color:#FF91B5;
}
list_description{display:inline;}
</style>
</head>
</html>
<div id="product_list" >
<h1>Listing Nokia products</h1>

<table>
<% @products.each do |product| %>
<tr class="<%= cycle('list-line-odd', 'list-line-even') %>">

<td>
<%= image_tag product.image_url, :class => 'list_image' %>
</td>

<td class="list_description">
<dl>

<td><dt><%= product.title %></dt></td>
<td><dd><%= truncate(strip_tags(product.description.gsub(/<.*?>/,'')),
:length => 80) %></dd></td>
</dl></td>
</td>
<td><dt><%= product.price %></dt></td>

<td class="list_actions">
<%= link_to 'Show', product %><br />
<%= link_to 'Edit', edit_product_path(product) %><br />
<%= link_to 'Remove', product, :confirm => 'Are you sure?',
:method => :delete %>
</td>
</tr>

<% end %>
</table>

</div>
<br />
<%= link_to 'New product', new_product_path %>
